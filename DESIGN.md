# 5문장 길찾기 디자인

## 0. 디자인 방향

학생이 처음 떠올린 한 문장에서 출발해, 자신의 경험을 다섯 문장으로 차례로 남기는 짧은 탐험이다. 매 단계는 앞 문장을 기계적으로 덧붙이는 시간이 아니라, 처음에 놓쳤던 장면과 느낌, 사람을 새로 발견하는 시간이다. 콘페스타의 보라색 무대 위에서 디디는 답을 주는 주인공이 아니라, 학생 곁에서 질문을 건네는 동반자다.

참조: `/Volumes/exDisk/vibecoding project/17. AI_MC/DESIGN.md`

## 1. 색과 재질

```css
:root {
  --grape: #8e14dc;
  --hero-bg: oklch(0.42 0.28 295);
  --lime: #c8ff3d;
  --pink: #ff00a9;
  --blue: #467aff;
  --mango: #ffd11e;
  --vanilla: #fdf8ff;
  --page: #fdfbff;
  --ink: #140a29;
  --muted: #5a5176;
  --border: #ddd2eb;
  --danger: #bb2649;
  --shadow-soft: 0 8px 24px -12px #1314281a;
  --shadow-pop: 0 20px 40px -16px #1314282e;
}
```

학생 화면은 깊은 보라색 배경에 라임·분홍빛·푸른빛을 작은 신호로 쓴다. 교사 대시보드는 바닐라색 바탕에 보라색 제목과 라임 버튼을 쓴다. 음영과 그림자는 깊이를 주되, 정보 읽기를 방해하지 않는다.

## 2. 글자

Paperlogy를 쓴다. 본문은 16px 이상, 화면 제목은 800 또는 900 굵기를 쓴다. 학생이 쓰는 문장은 흰색으로 두고 `0.65px` 검은 외곽선과 약한 그림자를 더한다. 본문에는 빽빽한 자간이나 과한 대문자 처리를 쓰지 않는다.

## 3. 화면의 순서

학생 화면은 위에서부터 현재 단계, 질문 말풍선, 캐릭터, 작성 칸 순으로 읽힌다. 학생은 한 번에 다음 문장 하나만 쓴다. 저장된 문장은 번호 카드로 남고, 그 다음 빈 입력칸만 열린다. 모바일에서 `지금까지 쓴 문장` 보관함은 `문장 저장` 버튼 아래에 둬 디디가 머무는 영역을 가리지 않는다. 다섯 카드가 모이면 먼저 `이미 여러분은 훌륭한 글을 쓸 준비가 되었어요.`라고 칭찬하고, 학생이 원할 때 디디의 마법으로 문장 사이를 이어 읽어 본다. 원문과 다듬어 볼 곳은 함께 남기며, 완료 뒤에도 각 카드는 수정할 수 있다.

입력칸 바로 아래에는 `이름·연락처·학교·학급·주소는 쓰지 않아요.`라는 짧은 안내를 둔다. 개인정보로 보이는 문장은 저장하지 않고 다시 쓰도록 안내한다. 작성 중인 초안은 저장하지 않는다. 도움을 요청할 때도 확정 문장과 초안 원문은 외부로 보내지 않는다. 기기 안에서 현재 단계, 문장 수, 초안 길이, 장면·감각·행동·관점 변화 신호, 반복 여부만 만들고 전송한다. Solar는 안전 질문의 방향 하나만 고른다. 학생 기기에서는 그 방향에 맞춰 첫 문장이나 바로 앞 문장을 짧게 짚고, 다음 장면·감각·행동·생각을 쉬운 말로 묻는다. 두 번째 문장에서는 첫 문장에 담긴 이동·도착·날씨·사람의 맥락을 기기 안에서만 읽어, 예를 들어 버스를 탔다면 버스 안이나 창밖을 묻는다. 답, 문장 작성·수정, 예시, 평가, 신원 요청이 섞인 자유 생성 응답은 보여 주지 않는다. 요청 실패, 서비스 이용 불가, 안전하지 않은 응답일 때도 같은 기기 안의 문맥 질문을 먼저 쓰고, 첫 문장이 없는 경우에만 순환하는 준비 질문으로 바꾼다. 말풍선은 화면 상단에 독립적으로 두고 캐릭터의 얼굴이나 몸을 가리지 않는다. 좁은 화면에서는 디디의 오른쪽 고정 영역을 말풍선이 침범하지 않게 남기고, 디디의 발끝도 작성대보다 위에 머물게 한다. 말풍선 배경은 글자가 선명한 범위에서만 살짝 비친다. 도움 버튼은 하단 작성 칸 위에 놓으며 최소 44px 터치 영역을 보장한다.

교사 대시보드는 실시간으로 바뀌며, 현재 앱이 기록하는 도움 요청(`help_requested`), 작성 중(`writing`), 완료(`completed`) 상태를 보여 준다. 색만으로 상태를 구별하지 않고 상태 글자도 함께 둔다. 진행 중 수업의 학생 참여 QR은 같은 내용을 담은 PNG 이미지로 내려받을 수 있다.

## 4. 캐릭터와 움직임

디디는 `robot-animated.glb` 3D 모델을 쓴다. 카메라와 모델은 정면으로 고정하고, 모델의 가장 낮은 지점은 3D 무대 바닥에 맞춘다. 아무것도 적지 않았을 때는 잔잔한 idle 상태로 기다리고, 생각 중·도움 요청·응원 상태에서는 팔 본에 들어 있는 트랙만 재생한다. 걷기·점프 클립에 섞여 있는 루트와 다리 트랙은 빼서 캐릭터가 자리를 벗어나지 않게 한다.

처음에는 디디가 가운데에서 조용히 기다린다. 10초 동안 입력이 없으면 같은 3D 모습 그대로 가벼운 달리기 몸짓을 이어, 처음 있던 자리에서 오른쪽 고정 자리까지 천천히 이동한다. 자동 안내 말풍선은 이동이 끝난 뒤에만 연다. 다만 학생이 이동 중 직접 `도움!`을 누르면 기다리게 하지 않고 바로 도움 질문을 연다. 루트와 다리 트랙은 계속 빼므로, 위치가 화면에서 옮겨도 발은 3D 무대 바닥에 붙어 있고 화면에서 사라지거나 다른 이미지로 바뀌지 않는다. 말풍선은 캐릭터와 겹치지 않도록 왼쪽에 두고, 캐릭터는 오른쪽 고정 영역에서만 몸짓한다.

디디가 질문을 읽는 동안에는 원본 프로젝트의 얼굴 프레임을 글자 수에 맞춰 순환해 더빙처럼 보이게 하고, 팔 전용 제스처와 몸의 작은 호흡을 함께 쓴다. 실제 음성은 재생하지 않는다.

완료 화면에서는 디디를 돌아보기 말풍선 아래, 학생의 완성 문단 위에 둔다. 두 요소와 겹치지 않도록 여백을 남기고, 내용이 길어져도 디디의 크기는 줄지 않는다. `디디의 마법 펼치기`를 누르면 상단 말풍선이 시전 상태를 알리고, 디디는 축하 제스처를 한다. 라임·망고·분홍빛 별가루와 고리가 디디 주위를 한 번만 돈다. 이어 읽기 버전은 움직임이 끝난 뒤 나타난다. 원래 문장 자체는 바꾸지 않고, 문단 흐름이 보이도록 짧은 연결어만 기기 안에서 더한다. 외부 AI 호출·저장·전송은 하지 않는다. 바로 아래에는 장면을 또렷하게 하는 감각, 인물·행동, 마지막 생각 중 한 곳을 골라 원문에 스스로 보탤 수 있는 짧은 질문을 보여 준다.

## 5. 재사용 요소와 상태

- 진행 표시: 1~5단계를 숫자와 점으로 함께 보인다.
- 문장 카드와 입력칸: 저장된 번호 카드, 다음 빈 입력칸, 수정 상태가 있다. 완료 뒤에도 저장된 카드를 고칠 수 있다.
- 질문 말풍선: 기본, `디디가 문장 사이를 살펴보고 있어요.`를 보이는 생성 중, 순환 안전 질문 상태가 있다.
- 도움 버튼: 기본, 요청 중, 비활성 상태가 있다.
- 완성 마법: 시작 전 칭찬과 `디디의 마법 펼치기` 버튼, 진행 중 축하 제스처와 장식 효과, 완료 뒤 원문을 보존한 이어 읽기 버전과 다듬기 질문 상태가 있다. 마법을 다시 펼쳐도 학생 글을 저장하거나 바꾸지 않는다.
- 교사 학생 카드: 현재 기록된 도움 요청(`help_requested`), 작성 중(`writing`), 완료(`completed`) 상태가 있다.
- 삭제 확인: 교사는 참여자 기록과 수업 전체 기록을 각각 확인 대화상자에서 한 번 더 확인한 뒤, 한 번의 명시적 확인으로 삭제할 수 있다. 수업 전체 정리는 먼저 `archiving`으로 학생 참여를 멈추고, 참여자(`participants`), 도움 요청(`helpRequests`), 응원(`cheers`), 개별 삭제 표시를 지운다. 모두 끝난 경우에만 `archived`로 보관하며, 실패하면 같은 정리를 다시 시도한다. 보관함에는 수업 시각만 남기고 학생 기록은 남기지 않는다. 보관한 수업을 완전히 지울 때도 확인 창을 거치며, 남아 있을 수 있는 하위 기록을 한 번 더 정리한 뒤 수업 시각과 보관함 항목까지 지운다. 삭제는 교사만 할 수 있으며, 학생은 다른 참여자의 기록을 삭제할 수 없다.

## 6. 반응형과 접근성

375px에서는 한 열, 768px 이상에서는 교사 카드의 두 열 배치, 1280px 이상에서는 넓은 대시보드 그리드를 쓴다. 가로 스크롤은 만들지 않는다. 모든 입력칸에는 보이는 레이블을 두고, 오류는 입력칸 가까이에 알린다. 색 대비는 일반 본문 4.5:1 이상을 유지한다.

삭제 확인 창은 참여자 기록 또는 수업 전체 기록 중 정리 범위를 알린다. 수업 전체 정리는 `기록 삭제하고 수업 보관`으로 표시하고 400개씩 나누어 처리하며, 현재 진행 상태와 실패 여부를 알린다. `prefers-reduced-motion`에서는 마법 효과를 건너뛰고 이어 읽기 버전을 바로 열며, 상태 변화는 글로도 알린다. 자세한 상태와 QR 흐름은 [수업 연결과 보관 흐름](docs/session-archive-lifecycle.md)을 따른다.
