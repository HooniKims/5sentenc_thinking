# 5문장 길찾기 디자인

## 0. 디자인 방향

학생이 처음 떠올린 한 문장에서 출발해, 주변을 천천히 더 살피며 다섯 문장까지 나아가는 짧은 탐험이다. 콘페스타의 보라색 무대 위에서 AI MC 캐릭터는 답을 주는 주인공이 아니라, 학생 곁에서 질문을 건네는 동반자다.

참조: `/Volumes/exDisk/vibecoding project/17. AI_MC/DESIGN.md`

## 1. 색과 재질

```css
:root {
  --grape: #8e14dc;
  --hero-bg: oklch(0.42 0.28 295);
  --lime: #c8ff3d;
  --pink: #ff00a9;
  --blue: #467aff;
  --mango: #ffd11e;
  --vanilla: #fdf8ff;
  --page: #fdfbff;
  --ink: #140a29;
  --muted: #5a5176;
  --border: #ddd2eb;
  --danger: #bb2649;
  --shadow-soft: 0 8px 24px -12px #1314281a;
  --shadow-pop: 0 20px 40px -16px #1314282e;
}
```

학생 화면은 깊은 보라색 배경에 라임·분홍빛·푸른빛을 작은 신호로 쓴다. 교사 대시보드는 바닐라색 바탕에 보라색 제목과 라임 버튼을 쓴다. 음영과 그림자는 깊이를 주되, 정보 읽기를 방해하지 않는다.

## 2. 글자

Paperlogy를 쓴다. 본문은 16px 이상, 화면 제목은 800 또는 900 굵기를 쓴다. 학생이 쓰는 문장은 흰색으로 두고 `0.65px` 검은 외곽선과 약한 그림자를 더한다. 본문에는 빽빽한 자간이나 과한 대문자 처리를 쓰지 않는다.

## 3. 화면의 순서

학생 화면은 위에서부터 현재 단계, 질문 말풍선, 캐릭터, 작성 칸 순으로 읽힌다. 말풍선은 화면 상단에 독립적으로 두고 캐릭터의 얼굴이나 몸을 가리지 않는다. 도움 버튼은 하단 작성 칸 위에 놓으며 최소 44px 터치 영역을 보장한다.

교사 대시보드는 도움 요청을 가장 먼저 보여 준다. 도움 요청 카드, 질문 생성 중 카드, 질문 전달 뒤 다음 문장을 기다리는 카드, 작성 중 카드, 완성 카드 순서다. 색만으로 상태를 구별하지 않고 상태 글자도 함께 둔다.

## 4. 캐릭터와 움직임

캐릭터는 `pose_think.png`, `pose_listen.png`, `pose_delight.png`, `pose_wave.png`을 쓴다. 생각 중에는 작은 위아래 움직임, 도움 질문과 응원 도착에는 짧은 나타남 효과만 쓴다. 움직임은 `transform`과 `opacity`만 사용하며, `prefers-reduced-motion`에서는 멈춘다.

## 5. 재사용 요소와 상태

- 진행 표시: 1~5단계를 숫자와 점으로 함께 보인다.
- 질문 말풍선: 기본, 생성 중, 오류 때 안전 질문 상태가 있다.
- 학생 입력칸: 빈 상태, 입력 중, 저장됨, 저장 실패 상태가 있다.
- 도움 버튼: 기본, 요청 중, 비활성 상태가 있다.
- 교사 학생 카드: 도움 요청, 질문 생성 중, 질문 전달, 작성 중, 완성 상태가 있다.
- 응원 카드: AI 초안, 전송 가능, 전송됨, 오류 상태가 있다.

## 6. 반응형과 접근성

375px에서는 한 열, 768px 이상에서는 교사 카드의 두 열 배치, 1280px 이상에서는 넓은 대시보드 그리드를 쓴다. 가로 스크롤은 만들지 않는다. 모든 입력칸에는 보이는 레이블을 두고, 오류는 입력칸 가까이에 알린다. 색 대비는 일반 본문 4.5:1 이상을 유지한다.
